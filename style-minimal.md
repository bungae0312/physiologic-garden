# 카드뉴스 디자인 시스템 문서

## 1. 캔버스 규격
- **크기**: 1080 x 1350px (4:5 비율, 인스타 세로형 권장 규격)
- **여백(safe zone)**: 상하좌우 최소 60px는 텍스트/핵심 요소 배치 금지 (인스타 UI에 가려질 수 있음)

## 2. 슬라이드 구성 순서
| 순서 | 유형 | 역할 |
|---|---|---|
| 1 | 표지(Cover) | 주제 제시, 시선 끌기 |
| 2 | 문제 제기 | 공감 유도, 궁금증 자극 |
| 3~N-1 | 본문(Content) | 정보 전달 (한 장당 1개 포인트) |
| N | 요약/CTA | 핵심 요약 + 팔로우·저장 유도 |

## 3. 색상 팔레트
| 용도 | 컬러명 | HEX |
|---|---|---|
| 배경 메인 | Primary BG | `#0B0B0D` (거의 블랙, 순검정보다 눈부심 적음) |
| 배경 서브(카드/박스) | Secondary BG | `#18181C` |
| 강조 텍스트/포인트/배지/보더 | Accent | `#6C6CF0` (인디고 퍼플 — 화이트와 대비 뚜렷, 가독성 좋음) |
| 본문 텍스트 | Text Body | `#FFFFFF` |
| 보조 텍스트(캡션, 출처) | Text Muted | `#9CA0AA` |
| 성공/체크 포인트(선택) | Success | `#4ADE80` |

> 배경은 순수 블랙(#000) 대신 `#0B0B0D`를 써서 눈부심을 줄이고, 텍스트는 순백(#FFFFFF)으로 대비를 최대화해 모바일 화면에서도 또렷하게 보이도록 함.

## 4. 타이포그래피
| 요소 | 폰트 | 크기(px) | 굵기 | 비고 |
|---|---|---|---|---|
| 표지 대제목 | Pretendard | 88~96 | ExtraBold(800) | 임팩트 최우선 요소, 2줄 이내, 줄간격 1.16 |
| 본문 슬라이드 제목(key title) | Pretendard | 76~80 | ExtraBold(800) | 2줄 이내, 줄간격 1.2. 표지보다 한 단계 작지만 임팩트가 확실히 느껴질 만큼 크게 — 80px보다 더 키우면 기존에 만든 카드들의 가장 긴 제목 줄(한글 11자 안팎)이 콘텐츠 폭(888px)을 넘길 수 있으니, 더 키울 때는 반드시 CLAUDE.md 3번 "줄바꿈 안전폭" 공식으로 재검산 |
| 본문 텍스트 | Pretendard | 44~46 | Medium(500) | **Text Body(#FFFFFF) 컬러 사용** — 캡션과 달리 muted 컬러를 쓰지 않는다. 줄간격 1.45 |
| 강조 키워드 | Pretendard | 본문과 동일~1.2배 | Bold(700) | Accent 컬러 or 밑줄 강조 |
| 상단 라벨/eyebrow("POINT 01" 등) | Pretendard | 28~30 | ExtraBold(800) | Accent 컬러, letter-spacing 3~4px, 대문자 |
| 캡션/출처 | Pretendard | 22~26 | Regular(400) | Text Muted 컬러 |
| 페이지 번호("01/06") | Pretendard | 24~28 | SemiBold(600) | 우상단 고정, Text Muted 컬러 |
| 배지 텍스트("SKILL 01" 등) | Pretendard | 28~30 | ExtraBold(800) | Accent 그라데이션 배경 + 화이트 텍스트 + 은은한 글로우(box-shadow), pill 형태(radius 999px) — 아래 6번 참고 |
| Before/After 비교 제목 | Pretendard | 56~60 | ExtraBold(800) | key title보다 한 단계 작게 — 아래 Before/After 박스가 같은 슬라이드에 들어가므로 |
| Before/After 라벨("BEFORE"/"AFTER") | Pretendard | 22~26 | ExtraBold(800) | letter-spacing 2px, 대문자. BEFORE는 Text Muted, AFTER는 Accent 컬러 |
| Before/After 박스 텍스트 | Pretendard | 30~34 | Medium(500) | BEFORE 박스 안 텍스트는 Text Muted, AFTER 박스 안 텍스트는 Text Body(흰색) |
| 인용구 박스(quote box) 텍스트 | Pretendard | 34~38 | Bold(700) | 슬라이드 안에서 "이 한 줄만 기억하면 됨" 식으로 따로 강조할 문구 전용 |

> Pretendard는 한글 가독성이 좋고 Bold 계열 웨이트가 풍부해 이런 임팩트형 카드뉴스에 널리 쓰이는 폰트. CDN(`https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css`)으로 바로 불러올 수 있음.

## 5. 레이아웃 규칙
- **정렬 기준**: 좌측 정렬 (제목/본문 모두 왼쪽 기준선 통일 — 읽는 흐름이 자연스럽고 모바일에서 안정적). 이 왼쪽 기준선은 본문 텍스트뿐 아니라 좌상단 로고/계정명, 우상단 페이지 번호까지 **슬라이드 전체가 동일한 좌우 마진(8~10%)을 공유**해야 한다 — 헤더 요소만 더 좁은 여백(예: 60px 세이프존 최소값)을 쓰면 본문과 기준선이 어긋나 보인다.
- **콘텐츠 영역**: 캔버스 대비 상하 15%, 좌우 8~10% 마진 확보
- **세로 배치 = 상단 고정(top-anchor), 중앙 정렬 금지**: 제목 블록은 콘텐츠 영역의 상단(캔버스 상단 기준 약 15%, 1080x1350 기준 약 210px 지점)에서 시작해 아래로 자연스럽게 흘러야 한다. 슬라이드마다 텍스트 분량이 달라도 시작 위치는 항상 동일해야 하며, 남는 공간은 하단에 남긴다. 콘텐츠를 세로 중앙 정렬(`justify-content:center`)하면 텍스트 분량에 따라 제목 시작 위치가 슬라이드마다 달라져 캐러셀을 넘길 때 제목이 위아래로 튀어 보이고 상하 여백이 불균형해지므로 금지한다.
- **한 슬라이드 = 한 메시지** 원칙 (텍스트 밀도 낮게, 정보 과다 금지). 콘텐츠 영역 하단에 남는 여백은 결함이 아니라 이 원칙에 따른 의도된 여백이다.
- **넘버링/인디케이터**: 우상단에 "01 / 06" 형식 페이지 번호, 좌상단에 로고/계정명 고정. 하단 중앙에 점(dot) 형태 진행 인디케이터

## 6. 공통 그래픽 요소
- **로고/계정 핸들**: 좌상단 고정 (예: "AI TREND"), 모든 슬라이드 동일 위치·크기
- **아이콘 스타일**: 심플한 필(fill) 아이콘 또는 화살표 정도만 최소한으로 사용 — 아이콘보다는 타이포그래피 자체의 임팩트에 집중
- **장식 요소**: 배경은 기본적으로 단색(#0B0B0D)이되, **모든 슬라이드 하단에 Accent 컬러의 은은한 radial-gradient 글로우**를 깔아 콘텐츠 아래 남는 여백이 비어 보이지 않고 은은한 분위기로 채워지도록 한다(예: `radial-gradient(ellipse 900px 520px at 50% 104%, rgba(108,108,240,0.35) 0%, transparent 70%)`). 표지 슬라이드는 여기에 좌상단·우상단 코너 글로우를 추가로 겹쳐 더 임팩트 있게 연출한다. 좌우 화살표(이전/다음) 버튼은 원형 반투명 배경 + 흰색 아이콘
- **구분선/카드형 박스**: Before/After 비교, 인용구 강조 등에 `Secondary BG(#18181C)` 배경 + `radius 16px` 박스 사용. 강조할 박스는 Accent 컬러(`#6C6CF0`) 2px 보더 + 은은한 글로우(`box-shadow`) 추가, 강조 안 할 박스(예: BEFORE)는 보더 없이 은은한 muted 톤 보더(`rgba(156,160,170,0.25)`) 1px만
- **Before/After 비교 박스**: 좌우 2단이 아니라 **위아래로 쌓는(stacked) 레이아웃**을 쓴다 — 한글 문장은 옆으로 나란히 두면 폭이 좁아져 줄바꿈이 많아지므로, 전체 콘텐츠 폭(888px)을 그대로 쓰는 stacked 방식이 가독성이 좋다. 순서: BEFORE 라벨 → BEFORE 박스(무강조) → AFTER 라벨 → AFTER 박스(Accent 보더 강조) → 설명 한 줄(박스 밖, Text Body 컬러)
- **인용구 박스(quote box)**: 본문 중간에 "이 한 줄만 기억하면 됨" 식으로 따로 복사해서 쓸 만한 문구를 강조할 때, Before/After의 AFTER 박스와 동일한 스타일(Secondary BG + Accent 보더 + 글로우)을 단독으로 사용
- **배지(Badge)**: pill 형태(radius 999px). 단색 Accent 대신 **Accent 그라데이션**(`linear-gradient(135deg, #8484FF 0%, #6C6CF0 100%)`) 배경 + 흰색 ExtraBold 텍스트 + **은은한 드롭 글로우**(`box-shadow: 0 10px 28px rgba(108,108,240,0.55), inset 0 0 0 1px rgba(255,255,255,0.16)`)로 태그/배지를 더 눈에 띄게 처리. 슬라이드 상단 제목 위에 배치

## 7. 슬라이드 유형별 템플릿

### 7-1. 표지(Cover)
- 대제목 + 소제목(선택) + 상징 아이콘/이미지
- 배경: 임팩트 있는 컬러/그라데이션

### 7-2. 본문(Content)
- 상단: 소제목 or 넘버링
- 중단: 핵심 텍스트 (텍스트 강조 컬러 활용)
- 하단: 보충 설명 or 출처 표기, 또는 인용구 박스(핵심 문장 하나를 따로 강조), 또는 아이콘

### 7-3. 요약/CTA
- 핵심 요약 리스트(3~5개 불릿) — 슬라이드 성격상 리스트가 필요 없으면(예: 마지막이 댓글 유도형 CTA) 생략하고 CTA 박스만 써도 됨
- 팔로우/저장/공유/댓글 유도 문구
- 계정 핸들 강조

### 7-4. Before/After 비교(Compare)
- 상단: 소제목 or 넘버링 (예: "CASE 01 · 글쓰기")
- 제목: 이 비교가 보여주는 함정/포인트를 한 문장으로
- 본문: BEFORE 박스 → AFTER 박스(Accent 강조) → 왜 다른지 설명하는 한 줄
- "프롬프트 전/후", "문장 전/후"처럼 같은 입력을 다르게 바꿨을 때의 결과 차이를 보여줄 때 사용

## 8. HTML/CSS 구현 시 참고사항
- 모든 색상·폰트 크기는 **CSS 변수(`:root`)**로 정의해 슬라이드 전체 일괄 수정 가능하도록 구성
- 슬라이드 1장 = `.slide` 클래스 1개 (1080x1350 고정 컨테이너)
- 텍스트는 플레이스홀더(`{{title}}`, `{{body}}` 등)로 처리해 데이터 주입 방식으로 렌더링
- 웹폰트 로딩 완료 후 스크린샷 촬영되도록 처리 (Playwright `waitForFunction(document.fonts.ready)` 등)
- **여백 기준값(1080x1350 기준)**: `.slide` 패딩은 상하 60px(세이프존 최소값) · 좌우 96px(9%)로 통일해서 헤더 행(로고/페이지번호)과 본문(제목/본문 텍스트)이 같은 좌우 기준선을 쓰도록 한다. 본문 컨텐츠 블록은 세로 중앙 정렬 대신 `padding-top: 170px`로 상단 고정해, 헤더 아래 약 270px(~20%) 지점에서 슬라이드 종류와 무관하게 항상 동일하게 시작하도록 한다. (처음엔 110px/약 210px이었는데, 제목·본문 폰트를 키우면서 제목이 헤더에 너무 붙어 보인다는 피드백을 받아 170px로 낮췄다 — 폰트 크기를 더 키울 경우 이 값도 같이 재조정 대상.)
- **배경 하단 글로우**: `.slide` 기본 스타일에 `background-image: radial-gradient(ellipse 900px 520px at 50% 104%, rgba(108,108,240,0.35) 0%, transparent 70%)`를 항상 적용. 표지(`.slide--cover`)는 여기에 좌상단(`circle at 26% 22%`)·우상단(`circle at 88% 10%`) 글로우 2개를 추가로 겹쳐 그린다. 텍스트는 상단(~270px)에서 시작하므로 하단 글로우가 가독성을 해치지 않는다.
- **텍스트 색상 주의**: 슬라이드 본문(핵심 문장 아래 보충 설명)은 출처/캡션이 아니라면 Text Muted가 아닌 **Text Body(#FFFFFF)**를 써야 임팩트가 유지된다 — muted는 22~26px 캡션·출처 전용으로 한정.

---
> ✅ 색상·폰트·정렬 값은 참고 이미지 스타일을 기반으로 가독성 우선으로 확정했습니다. 실제 제작 후 톤이 마음에 안 들면 CSS 변수만 바꿔서 쉽게 조정 가능합니다.
