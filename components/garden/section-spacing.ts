/**
 * ReferenceList/RelatedNotes가 공유하는 "다음 섹션 경계" 스타일 —
 * 두 파일에 같은 문자열을 반복해서 박아두지 않고 한 곳에서만 정의한다.
 * ArticlePagination은 docs/07 §7에 따라 시각적 비중을 의도적으로 더
 * 낮춰야 해서(pt-6) 이 상수를 공유하지 않고 자체 값을 쓴다.
 */
export const ARTICLE_SECTION_CLASS = "mt-16 border-t border-border pt-8";
